/// Exporter module for HTML export and screenshot
pub struct Exporter;

impl Exporter {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Exporter {
    fn default() -> Self {
        Self::new()
    }
}
